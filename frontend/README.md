# Tournament Hub Frontend

Next.js 14 App Router + TypeScript.

## Run

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

Open http://localhost:3000

Design tokens live in `styles/tokens.css` and are imported by `styles/globals.css`.
