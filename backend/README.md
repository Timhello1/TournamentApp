# Tournament Hub API

ASP.NET Core 8 + EF Core.

- Local: SQLite (`ConnectionStrings:Default` = `Data Source=tournament.db`)
- Production: set `ConnectionStrings__Default` (or `DATABASE_URL`) to a Neon Postgres URI

See root README for Neon + Render setup.